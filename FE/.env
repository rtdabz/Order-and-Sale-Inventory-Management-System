# The POS runs on a single till, so there are no remote terminals to broadcast
# to. Pusher / websocket configuration was removed along with the PC ordering
# feature; add VITE_ variables here if new client configuration is needed.
